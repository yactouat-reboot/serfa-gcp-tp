steps:

  # ! build
  # installation des dépendances 
  - name: python
    entrypoint: pip
    args: ["install", "-r", "requirements.txt", "--user"]
    id: "Install"

    # ! test
    # TODO

    # ! deploy
    # TODO
